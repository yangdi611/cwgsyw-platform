# v0.29.0 — 2026年7月3日

本次为功能性小版本，变更文档模板新增字段类型。

## 新增

- **变更文档模板字段类型新增「日期时间」** — 管理员在配置变更文档模板字段时，字段类型下拉框中新增「日期时间」选项。选择该类型后，填写变更文档表单时会渲染成日期+时间选择器，可精确到分钟，适用于需要记录具体操作时刻（而非仅日期）的场景。

## 兼容性

- 本次新增不影响已有模板和已提交的变更文档数据。
- 仅涉及变更文档模板模块，CMDB 模型属性字段类型不受影响。
