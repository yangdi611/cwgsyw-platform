# cwgsyw-platform 测试用例

## 使用说明

本文档包含平台全部模块的手工测试用例，按模块组织。每条用例包括：ID、优先级、前置条件、测试步骤、预期结果、清理、备注。

### 优先级说明
- **P0** — 核心流程，阻塞上线，必须全部通过
- **P1** — 重要功能/边界/权限，本迭代修复
- **P2** — 体验优化，可评估延期
- **P3** — 建议项，排入后续迭代

### 执行要求
- P0 用例：严格按照每一步操作执行并核对预期结果，不得跳跃
- P1 用例：至少按场景描述验证主流程
- P2 用例：选择性执行

### 用例模板

每条用例采用如下格式：

| 字段 | 值 |
|------|-----|
| **ID** | TC-AUTH-001 |
| **优先级** | P0 |
| **前置条件** | ... |
| **测试步骤** | 1. ... 2. ... |
| **预期结果** | 1. ... 2. ... |
| **清理** | ... |
| **备注** | ... |

---

## 一、认证模块 (TC-AUTH)

### TC-AUTH-001：正常登录

| 字段 | 值 |
|------|-----|
| **ID** | TC-AUTH-001 |
| **优先级** | P0 |
| **前置条件** | 1. 后端服务已启动，数据库已初始化（V3 种子数据包含 superadmin）<br>2. 前端服务已启动，可访问 `http://localhost:3000`<br>3. 浏览器清除 localStorage/cookies |
| **测试步骤** | 1. 打开浏览器，访问 `http://localhost:3000`<br>2. 确认页面跳转到 `/login`<br>3. 在用户名输入框输入 `superadmin`<br>4. 在密码输入框输入 `Admin@123`<br>5. 点击"登录"按钮 |
| **预期结果** | 1. 页面显示登录表单（居中卡片布局、Logo、用户名/密码输入框、登录按钮）<br>2. URL 为 `/login`<br>3. 用户名输入框接受输入<br>4. 密码显示为圆点（密文显示）<br>5. 跳转到 dashboard 首页 `/`，sidebar 显示全部菜单项，右上角显示用户名 `superadmin`，localStorage 中存在 `token` |
| **清理** | 在右上角用户菜单中点击"退出登录"，确认跳转回 `/login` |
| **备注** | 首次登录或 seed 后执行，确保数据正确 |

### TC-AUTH-002：错误密码提示

| 字段 | 值 |
|------|-----|
| **ID** | TC-AUTH-002 |
| **优先级** | P0 |
| **前置条件** | 用户 `superadmin` 存在于数据库，密码为 `Admin@123` |
| **测试步骤** | 1. 打开浏览器，访问 `/login`<br>2. 在用户名输入框输入 `superadmin`<br>3. 在密码输入框输入 `123`<br>4. 点击"登录"按钮 |
| **预期结果** | 1. 页面 URL 保持 `/login`，不会跳转<br>2. 页面显示错误提示信息："用户名或密码错误"<br>3. 输入框不清空，可立即重新输入 |
| **清理** | 无 |
| **备注** | 验证错误密码不应暴露具体是用户名不存在还是密码错误 |

### TC-AUTH-003：空表单校验

| 字段 | 值 |
|------|-----|
| **ID** | TC-AUTH-003 |
| **优先级** | P0 |
| **前置条件** | 页面已打开 `/login` |
| **测试步骤** | 1. 用户名和密码均留空，直接点击"登录"按钮<br>2. 仅填写用户名，密码留空，点击"登录"<br>3. 仅填写密码，用户名留空，点击"登录" |
| **预期结果** | 1. 登录按钮为 disabled 状态，或点击后提示"用户名不能为空"/"密码不能为空"<br>2. 不发起任何网络请求（查看 DevTools Network 面板确认）<br>3. 页面不跳转 |
| **清理** | 无 |
| **备注** | 前端校验，不应发请求到后端 |

### TC-AUTH-004：登出

| 字段 | 值 |
|------|-----|
| **ID** | TC-AUTH-004 |
| **优先级** | P0 |
| **前置条件** | 用户 `superadmin` 已成功登录，处于 dashboard 页面 |
| **测试步骤** | 1. 点击右上角用户头像/用户名<br>2. 在下拉菜单中点击"退出登录"或"登出"<br>3. 确认跳转回 `/login`<br>4. 打开浏览器 DevTools Application → LocalStorage，查看 `token`<br>5. 在地址栏直接输入 `http://localhost:3000/files` 并回车 |
| **预期结果** | 1. 下拉菜单出现，包含退出登录选项<br>2. 确认弹窗出现（如有）<br>3. 页面跳转到 `/login`<br>4. `token` 已从 localStorage 中清除<br>5. 重定向到 `/login`，不能直接访问受保护页面 |
| **清理** | 无 |
| **备注** | 验证 token 清除和路由守卫两重保护 |

### TC-AUTH-005：未登录访问保护

| 字段 | 值 |
|------|-----|
| **ID** | TC-AUTH-005 |
| **优先级** | P0 |
| **前置条件** | 1. 当前未登录，localStorage 无 token<br>2. 或使用隐私模式/清除浏览器数据的新标签页 |
| **测试步骤** | 1. 打开新标签页，直接访问 `http://localhost:3000/cmdb`<br>2. 查看 URL<br>3. 直接访问 `http://localhost:3000/files`<br>4. 直接访问 `http://localhost:3000/api/files`（API 路径） |
| **预期结果** | 1. 页面重定向到 `/login`，URL 变为 `http://localhost:3000/login`<br>2. 无 protected 页面内容泄露<br>3. 同理重定向到 `/login`<br>4. API 返回 401 未授权 |
| **清理** | 无 |
| **备注** | 前后端双重保护：前端路由守卫 + 后端 JWT 过滤器 |

### TC-AUTH-006：不存在用户

| 字段 | 值 |
|------|-----|
| **ID** | TC-AUTH-006 |
| **优先级** | P1 |
| **前置条件** | 数据库中不存在用户名为 `test999` 的用户 |
| **测试步骤** | 1. 打开 `/login`<br>2. 输入用户名 `test999`，密码任意<br>3. 点击登录 |
| **预期结果** | 1. 提示"用户名或密码错误"<br>2. 不透露"用户不存在"的具体信息 |
| **清理** | 无 |
| **备注** | 安全原则：不区分用户不存在和密码错误 |

### TC-AUTH-007：SQL 注入防护

| 字段 | 值 |
|------|-----|
| **ID** | TC-AUTH-007 |
| **优先级** | P1 |
| **前置条件** | `/login` 页面已打开 |
| **测试步骤** | 1. 用户名输入 `' OR '1'='1`，密码输入任意值，点击登录<br>2. 用户名输入 `admin'--`，密码输入任意值，点击登录<br>3. 用户名输入 `; DROP TABLE sys_user;--`，密码任意，点击登录 |
| **预期结果** | 1. 均提示"用户名或密码错误"<br>2. 不会登录成功<br>3. 数据库 sys_user 表不受影响，系统正常运行 |
| **清理** | 检查 sys_user 表数据是否完好 |
| **备注** | 后端应使用参数化查询，MyBatis `#{}` 自动防注入 |

### TC-AUTH-008：连续错误密码不锁定

| 字段 | 值 |
|------|-----|
| **ID** | TC-AUTH-008 |
| **优先级** | P1 |
| **前置条件** | `superadmin` 用户存在 |
| **测试步骤** | 1. 连续 5 次输入错误密码（用户名 `superadmin`，密码任意错误值）<br>2. 第 6 次输入正确密码 `Admin@123` |
| **预期结果** | 1. 每次错误密码都提示"用户名或密码错误"<br>2. 不出验证码、不锁定账户、不需要等待<br>3. 第 6 次正确密码成功登录进入 dashboard |
| **清理** | 成功后登出 |
| **备注** | 当前 MVP 阶段未实现登录锁定策略，后续可引入 |

### TC-AUTH-009：登录页 UI

| 字段 | 值 |
|------|-----|
| **ID** | TC-AUTH-009 |
| **优先级** | P2 |
| **前置条件** | 浏览器打开 `/login` |
| **测试步骤** | 1. 观察页面布局<br>2. 查看页面加载后焦点位置<br>3. 不操作鼠标，直接按键盘 Enter<br>4. 在不同分辨率下查看布局（1440px、1024px、375px） |
| **预期结果** | 1. 页面居中卡片布局，显示平台 Logo 和名称<br>2. 用户名输入框默认获得焦点（光标闪烁）<br>3. Enter 键提交表单触发空表单校验<br>4. 响应式：移动端卡片全宽，桌面端居中 |
| **清理** | 无 |
| **备注** | 纯 UI 检查，无功能异常也算通过 |

### TC-AUTH-010：密码可见性切换

| 字段 | 值 |
|------|-----|
| **ID** | TC-AUTH-010 |
| **优先级** | P2 |
| **前置条件** | `/login` 页面已打开 |
| **测试步骤** | 1. 在密码输入框输入任意字符 `MyPassword123`<br>2. 点击密码框右侧的眼睛图标<br>3. 再次点击眼睛图标 |
| **预期结果** | 1. 默认密码显示为圆点（`type="password"`）<br>2. 点击后密码变为明文显示：`MyPassword123`<br>3. 再次点击恢复为圆点显示 |
| **清理** | 无 |
| **备注** | 关注图标状态切换（睁眼/闭眼） |

---

## 二、权限模块 (TC-RBAC)

### TC-RBAC-001：superadmin 全模块可见

| 字段 | 值 |
|------|-----|
| **ID** | TC-RBAC-001 |
| **优先级** | P0 |
| **前置条件** | 以 `superadmin` / `Admin@123` 成功登录 |
| **测试步骤** | 1. 查看左侧 sidebar 菜单<br>2. 逐项展开所有菜单分组<br>3. 逐一点击各菜单项确认可正常访问 |
| **预期结果** | 1. Sidebar 显示的菜单项包括但不限于：用户管理、组管理、日报管理、设备管理、通知中心、AI 配置、变更文档、审计日志、CMDB（含模型配置、实例管理）、文件管理<br>2. 每个菜单项点击后跳转到对应页面，页面正常加载<br>3. 所有页面上操作按钮（新建/编辑/删除等）均可见且可用 |
| **清理** | 无 |
| **备注** | superadmin 拥有 `platform` 级别 `groupScope`，全部资源的所有 action |

### TC-RBAC-002：member 有限菜单

| 字段 | 值 |
|------|-----|
| **ID** | TC-RBAC-002 |
| **优先级** | P0 |
| **前置条件** | 1. 存在一个 member 角色的用户（如 `member1`）<br>2. 该用户属于某个组<br>3. 以该用户身份登录 |
| **测试步骤** | 1. 查看左侧 sidebar 菜单<br>2. 记录可见菜单项<br>3. 对比 superadmin 的菜单列表 |
| **预期结果** | 1. member 角色看不到"系统管理"类菜单（如角色管理、系统配置等）<br>2. member 能看到但不包含管理操作（如设备列表可见但无"新增设备"按钮）<br>3. 对比 superadmin，member 的 sidebar 项显著减少 |
| **清理** | 登出 |
| **备注** | 具体权限见 RBAC 模型中 member 的 role_permission 配置 |

### TC-RBAC-003：member 文件可见性

| 字段 | 值 |
|------|-----|
| **ID** | TC-RBAC-003 |
| **优先级** | P0 |
| **前置条件** | 1. 以 member1 身份登录（假设 member1 属于 group_id = 2）<br>2. 文件系统中同时存在：visible_groups 包含 [2] 的文件、visible_groups 为 null/空数组的文件、visible_groups 包含 [3] 的文件 |
| **测试步骤** | 1. 访问 `/files` 文件管理页<br>2. 查看文件列表和文件夹树<br>3. 尝试搜索或翻页查看更多文件 |
| **预期结果** | 1. 文件列表中只显示 `visible_groups` 包含本组（group_id=2）或为 null/空数组的文件<br>2. 文件夹树只显示包含可见文件/子文件夹的节点<br>3. `visible_groups = [3]` 的文件不可见 |
| **清理** | 登出 |
| **备注** | 后端 SharedFileService 实现了按组可见性过滤 |

### TC-RBAC-004：viewer 只读视图

| 字段 | 值 |
|------|-----|
| **ID** | TC-RBAC-004 |
| **优先级** | P0 |
| **前置条件** | 1. 存在一个 viewer 角色的用户（如 `viewer1`）<br>2. 以 viewer1 身份登录 |
| **测试步骤** | 1. 访问各个模块页面（如设备管理、文件管理、日报管理）<br>2. 检查页面中是否有"新建"、"编辑"、"删除"等操作按钮<br>3. 检查是否有"导入"、"导出"、"分享"、"管理"等入口 |
| **预期结果** | 1. 列表页数据可查看<br>2. 所有写操作按钮（新建/编辑/删除/导入/导出等）不可见或为 disabled 状态<br>3. 不显示系统管理、配置管理等相关入口 |
| **清理** | 登出 |
| **备注** | viewer 只有 read action 权限 |

### TC-RBAC-005：viewer POST 文件 403

| 字段 | 值 |
|------|-----|
| **ID** | TC-RBAC-005 |
| **优先级** | P1 |
| **前置条件** | 1. viewer1 已登录<br>2. 获取到 viewer1 的 JWT token（可从 DevTools Application → LocalStorage 中复制） |
| **测试步骤** | 1. 打开浏览器 DevTools<br>2. 使用 fetch 或 curl 手动发送 POST 请求到 `/api/files/upload`<br>3. 请求头携带 viewer1 的 token<br>4. 尝试发送有效的 FormData（含文件） |
| **预期结果** | 1. 后端返回 HTTP 403 Forbidden<br>2. 响应体包含错误信息，如 `"message": "Access Denied"` 或类似<br>3. 文件未被上传 |
| **清理** | 无 |
| **备注** | 验证后端 `@PreAuthorize` 注解生效。测试 URL 也可以是其他受保护的 POST/PUT/DELETE 接口 |

### TC-RBAC-006：member 无法访问 CMDB 配置管理

| 字段 | 值 |
|------|-----|
| **ID** | TC-RBAC-006 |
| **优先级** | P1 |
| **前置条件** | 以 member1 身份登录 |
| **测试步骤** | 1. 在地址栏直接输入 `http://localhost:3000/cmdb/admin`<br>2. 观察页面表现<br>3. 检查 sidebar 中是否有"CMDB 配置管理"入口 |
| **预期结果** | 1. 页面显示"无权限访问"或重定向到 CMDB 首页 `/cmdb`<br>2. sidebar 中 CMDB 菜单下只有"CI 资源"或"实例管理"等入口，无"模型配置"或"配置管理"<br>3. 后端 API 请求返回 403 |
| **清理** | 无 |
| **备注** | `cmdb_model:write` 权限仅分配给 admin/super_admin |

### TC-RBAC-007：member 无法看到"查看密码"按钮

| 字段 | 值 |
|------|-----|
| **ID** | TC-RBAC-007 |
| **优先级** | P1 |
| **前置条件** | 1. 以 member1 身份登录<br>2. 设备列表中有凭据数据 |
| **测试步骤** | 1. 访问设备管理页面<br>2. 查看设备列表每行右侧的操作按钮<br>3. 点击某设备进入详情页 |
| **预期结果** | 1. 列表行操作中没有"查看密码"或"复制密码"按钮<br>2. 详情页中没有查看密码按钮或链接<br>3. 通过 DevTools Network 查看，未请求密码相关 API |
| **清理** | 无 |
| **备注** | `device:view_password` 权限仅分配给 leader 及以上角色 |

### TC-RBAC-008：leader 仅看本组数据

| 字段 | 值 |
|------|-----|
| **ID** | TC-RBAC-008 |
| **优先级** | P1 |
| **前置条件** | 1. 存在 leader 角色的用户（如 `leader1`），属于 group_id = 2<br>2. 存在其他组（group_id = 1）的用户和设备数据 |
| **测试步骤** | 1. 以 leader1 身份登录<br>2. 访问用户管理页面<br>3. 访问设备管理页面<br>4. 访问文件管理页面 |
| **预期结果** | 1. 用户管理只显示 group_id = 2 的成员列表<br>2. 设备管理只显示属于 group_id = 2 或 leader1 可见的设备<br>3. 文件管理只显示 leader1 所属组可见的文件<br>4. 其他组的数据不可见 |
| **清理** | 登出 |
| **备注** | leader 的 `groupScope = group`，数据级权限由 Service 层过滤 |

### TC-RBAC-009：leader 可审批本组日报

| 字段 | 值 |
|------|-----|
| **ID** | TC-RBAC-009 |
| **优先级** | P1 |
| **前置条件** | 1. leader1 属于 group_id = 2<br>2. 存在至少一个 group_id = 2 的 member 提交的待审批日报<br>3. 存在其他组（group_id = 1）的待审批日报 |
| **测试步骤** | 1. 以 leader1 登录<br>2. 访问日报管理 → 审批页<br>3. 查看待审批列表<br>4. 尝试审批通过本组日报<br>5. 查看是否看到其他组日报 |
| **预期结果** | 1. 待审批列表只显示本组（group_id = 2）成员的日报<br>2. 其他组的日报不在列表中<br>3. 可成功审批通过本组日报，状态变更为"已通过" |
| **清理** | 无 |
| **备注** | 审批操作同时验证权限和数据范围 |

### TC-RBAC-010：无权限 API 返回 403

| 字段 | 值 |
|------|-----|
| **ID** | TC-RBAC-010 |
| **优先级** | P1 |
| **前置条件** | 1. 以低权限用户（viewer1/member1）登录<br>2. 从 DevTools 获取 JWT token |
| **测试步骤** | 1. 使用 curl 或 DevTools 控制台发送 curl 请求：<br>   `curl -X POST http://localhost:3000/api/files/folders`<br>   `-H "Authorization: Bearer <token>"`<br>   `-H "Content-Type: application/json"`<br>   `-d '{"name":"test","parent_id":null}'`<br>2. 尝试其他受保护的 POST/PUT/DELETE 接口 |
| **预期结果** | 1. 每个请求返回 HTTP 403<br>2. 响应体包含类似 `"error": "Access is denied"` 信息<br>3. 操作未生效（数据库无变化） |
| **清理** | 无 |
| **备注** | curl 命令可在 DevTools Console 中使用 `fetch()` 替代 |

### TC-RBAC-011：无权限页面提示友好

| 字段 | 值 |
|------|-----|
| **ID** | TC-RBAC-011 |
| **优先级** | P2 |
| **前置条件** | 以 viewer1 或 member1 身份登录 |
| **测试步骤** | 1. 直接访问无权限的页面 URL（如 `/cmdb/admin` 或 `/admin/users`）<br>2. 观察页面展示 |
| **预期结果** | 1. 页面显示友好的提示信息，如"无权限访问此页面"<br>2. 显示"返回首页"按钮或链接<br>3. 页面不至于白屏或显示空白 |
| **清理** | 无 |
| **备注** | 在 layout 层或 page 组件内有权限检查守卫 |

### TC-RBAC-012：Sidebar 菜单响应式

| 字段 | 值 |
|------|-----|
| **ID** | TC-RBAC-012 |
| **优先级** | P2 |
| **前置条件** | 以任意用户登录 dashboard |
| **测试步骤** | 1. 在大屏（1440px）下观察 sidebar<br>2. 逐步缩小浏览器窗口宽度<br>3. 点击菜单折叠按钮（汉堡图标）<br>4. 在移动端宽度（375px）下观察 |
| **预期结果** | 1. 大屏下 sidebar 展开显示文字 + 图标<br>2. 窗口缩小时 sidebar 可折叠为仅图标模式<br>3. 折叠/展开动画流畅<br>4. 移动端可滑动或切换显示 |
| **清理** | 无 |
| **备注** | 不要求完美移动端适配，但 core 交互无明显 bug |

---

## 三、用户管理模块 (TC-USER)

### TC-USER-001：新建用户

| 字段 | 值 |
|------|-----|
| **ID** | TC-USER-001 |
| **优先级** | P0 |
| **前置条件** | 1. 以 `superadmin` 登录<br>2. 导航到用户管理页面（sidebar → 用户管理 → 用户列表） |
| **测试步骤** | 1. 点击"新建用户"或"添加用户"按钮<br>2. 在弹出的表单/新页面中填写：<br>   - 用户名：`testuser01`<br>   - 真实姓名：`测试用户01`<br>   - 邮箱：`test01@example.com`<br>   - 密码：`Test@123`<br>   - 确认密码：`Test@123`<br>3. 选择一个所属组（可选，如有组字段）<br>4. 点击"保存"或"提交"按钮<br>5. 返回用户列表 |
| **预期结果** | 1. 保存成功后提示"创建成功"或类似成功消息<br>2. 用户列表中出现新用户 `testuser01`，显示真实姓名 `测试用户01`<br>3. 列表状态显示为"启用"<br>4. 使用 `testuser01` / `Test@123` 可成功登录 |
| **清理** | 删除新建用户 `testuser01`（硬删除或软删除），或在后续测试中使用 |
| **备注** | 验证密码加密存储，数据库中 password 字段应为 BCrypt hash |

### TC-USER-002：编辑用户信息

| 字段 | 值 |
|------|-----|
| **ID** | TC-USER-002 |
| **优先级** | P0 |
| **前置条件** | 1. 以 superadmin 登录<br>2. 用户列表中存在用户 `testuser01`（由 TC-USER-001 创建） |
| **测试步骤** | 1. 在用户列表找到 `testuser01`，点击"编辑"按钮<br>2. 修改真实姓名为 `测试用户01-已修改`<br>3. 修改邮箱为 `test01_modified@example.com`<br>4. 点击"保存" |
| **预期结果** | 1. 保存成功后提示"更新成功"<br>2. 用户列表刷新后，`testuser01` 的真实姓名显示为新值<br>3. 邮箱显示为新值<br>4. 密码未变更，仍可用原密码登录 |
| **清理** | 无 |
| **备注** | 编辑时密码字段应留空，留空不修改密码 |

### TC-USER-003：为用户分配角色

| 字段 | 值 |
|------|-----|
| **ID** | TC-USER-003 |
| **优先级** | P0 |
| **前置条件** | 1. 以 superadmin 登录<br>2. 用户 `testuser01` 存在，当前角色为默认角色（如 member） |
| **测试步骤** | 1. 在用户列表找到 `testuser01`，点击"编辑"或"角色分配"<br>2. 在角色分配区域，勾选 `member` 角色（如已有则确保勾选）<br>3. 点击"保存"<br>4. 登出当前用户<br>5. 以 `testuser01` / `Test@123` 登录 |
| **预期结果** | 1. 角色保存成功后提示"更新成功"<br>2. `testuser01` 登录后的 sidebar 和功能权限符合 member 角色的定义<br>3. 权限立即生效，无需重新部署 |
| **清理** | 登出 `testuser01`，回到 superadmin 账户 |
| **备注** | 若支持多角色，可勾选多个角色验证权限合并效果 |

### TC-USER-004：禁用/启用用户

| 字段 | 值 |
|------|-----|
| **ID** | TC-USER-004 |
| **优先级** | P0 |
| **前置条件** | 1. 以 superadmin 登录<br>2. 用户 `testuser01` 存在，当前为启用状态 |
| **测试步骤** | 1. 在用户列表找到 `testuser01`，点击"编辑"<br>2. 将"状态"改为"禁用"<br>3. 保存<br>4. 打开新标签页或登出<br>5. 以 `testuser01` / `Test@123` 尝试登录<br>6. 回到用户管理，将 `testuser01` 状态改回"启用"<br>7. 再次以 `testuser01` 登录 |
| **预期结果** | 1. 状态保存成功<br>2. `testuser01` 登录时提示"账号已禁用"或"账户已被禁用"<br>3. 无法进入 dashboard<br>4. 恢复启用后，`testuser01` 可正常登录 |
| **清理** | 确认 testuser01 最终为启用状态 |
| **备注** | 禁用不应删除用户数据和关联关系 |

### TC-USER-005：用户名重复

| 字段 | 值 |
|------|-----|
| **ID** | TC-USER-005 |
| **优先级** | P1 |
| **前置条件** | 1. 以 superadmin 登录<br>2. 用户 `superadmin` 已存在 |
| **测试步骤** | 1. 点击"新建用户"<br>2. 用户名输入 `superadmin`（已存在的用户名）<br>3. 填写其他必填字段<br>4. 点击"保存" |
| **预期结果** | 1. 保存失败<br>2. 提示"用户名已存在"或类似信息<br>3. 不会创建同名用户<br>4. 数据库中 `superadmin` 记录仍只有一条 |
| **清理** | 无 |
| **备注** | 后端应有唯一性校验 |

### TC-USER-006：邮箱格式校验

| 字段 | 值 |
|------|-----|
| **ID** | TC-USER-006 |
| **优先级** | P1 |
| **前置条件** | 1. 以 superadmin 登录<br>2. 点击"新建用户" |
| **测试步骤** | 1. 填写用户名、密码等必填字段<br>2. 邮箱输入 `notanemail`<br>3. 点击"保存"<br>4. 再尝试邮箱输入 `test@`<br>5. 点击"保存" |
| **预期结果** | 1. 第一次保存失败，提示"邮箱格式不正确"或类似信息<br>2. 第二次保存也失败，提示"邮箱格式不正确"<br>3. 页面不跳转，表单不回滚 |
| **清理** | 无 |
| **备注** | 前后端双重校验 |

### TC-USER-007：必填字段校验

| 字段 | 值 |
|------|-----|
| **ID** | TC-USER-007 |
| **优先级** | P1 |
| **前置条件** | 1. 以 superadmin 登录<br>2. 点击"新建用户" |
| **测试步骤** | 1. 不填写任何字段，直接点击"保存"<br>2. 仅填写用户名，其他字段留空，点击"保存"<br>3. 仅填写密码，其他字段留空，点击"保存" |
| **预期结果** | 1. 每次保存均失败<br>2. 提示"用户名不能为空"、"真实姓名不能为空"、"密码不能为空"等对应信息<br>3. 必填字段输入框有红色边框或星号标记 |
| **清理** | 无 |
| **备注** | 必填字段为：用户名、真实姓名、密码（新建时） |

### TC-USER-008：搜索用户

| 字段 | 值 |
|------|-----|
| **ID** | TC-USER-008 |
| **优先级** | P1 |
| **前置条件** | 1. 以 superadmin 登录<br>2. 用户列表中存在多个用户，包括含 `admin` 关键词的（superadmin、admin 等） |
| **测试步骤** | 1. 在搜索框中输入 `admin`<br>2. 查看搜索结果<br>3. 清空搜索框<br>4. 搜索一个不存在的关键词 `zzz999xxx`<br>5. 搜索用户名的一部分，如 `super` |
| **预期结果** | 1. 搜索 `admin` 时，列表筛选出用户名或真实姓名中包含 `admin` 的用户<br>2. 清空搜索框后，列表恢复显示全部用户<br>3. 搜索 `zzz999xxx` 时，列表显示"无数据"或空状态<br>4. 搜索 `super` 时，显示 `superadmin` |
| **清理** | 无 |
| **备注** | 搜索支持模糊匹配，匹配字段包含用户名和真实姓名 |

### TC-USER-009：用户列表分页

| 字段 | 值 |
|------|-----|
| **ID** | TC-USER-009 |
| **优先级** | P2 |
| **前置条件** | 1. 以 superadmin 登录<br>2. 用户总数超过 20（可分多次测试累积数据，或确认 seed 数据已达此数量） |
| **测试步骤** | 1. 访问用户管理列表<br>2. 查看列表底部是否有分页控件<br>3. 点击第 2 页<br>4. 调整每页显示条数（如从 10 改为 20）<br>5. 点击"下一页"和"上一页" |
| **预期结果** | 1. 分页控件显示总条数、当前页/总页数<br>2. 点击第 2 页后加载第 2 页数据，URL 参数携带 page=2<br>3. 调整每页条数后刷新列表<br>4. 翻页操作正常，数据不重复不遗漏 |
| **清理** | 无 |
| **备注** | 验证 total 计数正确（注意 MyBatis-Plus Boolean @TableLogic 的 count bug 处理） |

### TC-USER-010：列表排序

| 字段 | 值 |
|------|-----|
| **ID** | TC-USER-010 |
| **优先级** | P2 |
| **前置条件** | 1. 以 superadmin 登录<br>2. 用户列表有多个用户 |
| **测试步骤** | 1. 点击"用户名"表头<br>2. 再次点击"用户名"表头<br>3. 点击"创建时间"表头 |
| **预期结果** | 1. 第一次点击表头，列表按用户名升序排列<br>2. 第二次点击，切换为降序排列<br>3. 切换排序字段后按新字段排序<br>4. 表头显示排序方向箭头指示 |
| **清理** | 无 |
| **备注** | 排序为前端或后端排序视实现而定 |

---

## 四、组管理模块 (TC-GROUP)

### TC-GROUP-001：新建组

| 字段 | 值 |
|------|-----|
| **ID** | TC-GROUP-001 |
| **优先级** | P0 |
| **前置条件** | 1. 以 `superadmin` 登录<br>2. 导航到组管理页面（sidebar → 组管理）<br>3. 数据库中没有同名"测试组" |
| **测试步骤** | 1. 点击"新建组"或"添加组"按钮<br>2. 在表单中输入组名：`测试组`<br>3. 填写组描述（可选）：`这是一个测试组`<br>4. 点击"保存" |
| **预期结果** | 1. 保存成功后提示"创建成功"<br>2. 组列表中出现新组 `测试组`<br>3. 组名和描述正确显示<br>4. 刚创建的组默认无成员 |
| **清理** | 在后续用例中使用该组；或测试完成后删除 |
| **备注** | 组名在同一租户内应唯一 |

### TC-GROUP-002：编辑组名

| 字段 | 值 |
|------|-----|
| **ID** | TC-GROUP-002 |
| **优先级** | P0 |
| **前置条件** | 1. 以 superadmin 登录<br>2. 存在组 `测试组`（由 TC-GROUP-001 创建） |
| **测试步骤** | 1. 在组列表中找到 `测试组`，点击"编辑"按钮<br>2. 修改组名为 `测试组-已修改`<br>3. 修改组描述为 `已修改的描述`<br>4. 点击"保存" |
| **预期结果** | 1. 保存成功后提示"更新成功"<br>2. 列表刷新后组名显示为 `测试组-已修改`<br>3. 描述显示为新内容 |
| **清理** | 可将组名改回 `测试组` 以便后续用例使用 |
| **备注** | 编辑组不应影响组内成员 |

### TC-GROUP-003：添加成员

| 字段 | 值 |
|------|-----|
| **ID** | TC-GROUP-003 |
| **优先级** | P0 |
| **前置条件** | 1. 以 superadmin 登录<br>2. 存在组 `测试组`<br>3. 存在至少一个不属于该组的用户（如 `testuser01`） |
| **测试步骤** | 1. 在组列表中找到 `测试组`，点击组名进入组详情<br>2. 在组成员区域点击"添加成员"或"新增成员"<br>3. 在弹出的用户选择器中搜索或选择 `testuser01`<br>4. 确认选择<br>5. 查看组成员列表 |
| **预期结果** | 1. 添加成功后提示"添加成功"<br>2. 组成员列表中显示 `testuser01`<br>3. 再次打开添加成员弹窗，已添加的用户不应出现在可选列表中（或标记已添加） |
| **清理** | 无 |
| **备注** | 一个用户可以属于多个组 |

### TC-GROUP-004：移除成员

| 字段 | 值 |
|------|-----|
| **ID** | TC-GROUP-004 |
| **优先级** | P0 |
| **前置条件** | 1. 以 superadmin 登录<br>2. 组成员 `testuser01` 存在于 `测试组` 中 |
| **测试步骤** | 1. 进入 `测试组` 的组详情<br>2. 在组成员列表中找到 `testuser01`<br>3. 点击"移除"或"删除"按钮<br>4. 在确认弹窗中点击"确认" |
| **预期结果** | 1. 移除操作后提示"移除成功"<br>2. 组成员列表不再显示 `testuser01`<br>3. `testuser01` 仍然存在于其他组（如有）<br>4. `testuser01` 用户未被删除 |
| **清理** | 无 |
| **备注** | 仅移除组成员关系，不删除用户本身 |

### TC-GROUP-005：删除空组

| 字段 | 值 |
|------|-----|
| **ID** | TC-GROUP-005 |
| **优先级** | P1 |
| **前置条件** | 1. 以 superadmin 登录<br>2. 存在一个没有成员的空组（如新建一个"待删除组"，不添加任何成员） |
| **测试步骤** | 1. 在组列表中找到该空组<br>2. 点击"删除"按钮<br>3. 在确认弹窗中点击"确认" |
| **预期结果** | 1. 删除成功，提示"删除成功"<br>2. 组列表中不再显示该组<br>3. 数据库中使用逻辑删除（`is_deleted = 1`），非物理删除 |
| **清理** | 无 |
| **备注** | 验证软删除机制 |

### TC-GROUP-006：删除有成员的组

| 字段 | 值 |
|------|-----|
| **ID** | TC-GROUP-006 |
| **优先级** | P1 |
| **前置条件** | 1. 以 superadmin 登录<br>2. `测试组` 中至少有一个成员（如 `testuser01`） |
| **测试步骤** | 1. 在组列表中找到 `测试组`<br>2. 尝试点击"删除"按钮<br>3. 如不可删除，观察提示<br>4. 如可点击删除，在确认弹窗后观察结果 |
| **预期结果** | 1. 系统提示"请先移除组内所有成员后再删除"或"该组下有成员，无法删除"<br>2. 或者删除按钮为 disabled 状态并提示原因<br>3. 组未被删除，成员数据不受影响 |
| **清理** | 无 |
| **备注** | 保护有成员的组不被误删 |

### TC-GROUP-007：组列表搜索

| 字段 | 值 |
|------|-----|
| **ID** | TC-GROUP-007 |
| **优先级** | P2 |
| **前置条件** | 1. 以 superadmin 登录<br>2. 组列表中存在多个组，组名包含不同关键词 |
| **测试步骤** | 1. 在组列表搜索框中输入 `测试`<br>2. 查看搜索结果<br>3. 清空搜索框<br>4. 搜索一个不存在的组名 `zzz999`<br>5. 搜索部分组名如 `组` |
| **预期结果** | 1. 搜索 `测试` 时，显示组名中包含"测试"的组<br>2. 清空搜索框后恢复完整列表<br>3. 搜索 `zzz999` 时显示"无数据"空状态<br>4. 模糊匹配正常 |
| **清理** | 无 |
| **备注** | 搜索字段至少包括组名 |

### TC-GROUP-008：组列表分页

| 字段 | 值 |
|------|-----|
| **ID** | TC-GROUP-008 |
| **优先级** | P2 |
| **前置条件** | 1. 以 superadmin 登录<br>2. 组总数超过一页（根据默认每页条数，通常 > 10 或 > 20） |
| **测试步骤** | 1. 访问组管理列表<br>2. 查看分页控件<br>3. 切换到第 2 页<br>4. 切换每页条数<br>5. 点击下一页/上一页 |
| **预期结果** | 1. 分页控件正确显示总组数、当前页<br>2. 翻页加载数据正常，无重复或遗漏<br>3. 切换每页条数后列表刷新<br>4. 搜索状态下分页正确（搜索结果的 count） |
| **清理** | 无 |
| **备注** | 关注 MyBatis-Plus Boolean 逻辑删除字段的分页 total 问题 |

---

## 附录

### 测试数据参考

| 用户名 | 密码 | 角色 | 说明 |
|--------|------|------|------|
| `superadmin` | `Admin@123` | super_admin | 全平台权限 |
| `admin` | （由 seed 或管理员设置） | admin | 租户级管理员 |
| `leader1` | （由 seed 或管理员设置） | group_leader | 本组管理+审批权限 |
| `member1` | （由 seed 或管理员设置） | member | 基本读写权限 |
| `viewer1` | （由 seed 或管理员设置） | viewer | 只读权限 |

### 常用 API 端点（手动测试用）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/login` | 登录 |
| GET | `/api/users` | 用户列表 |
| POST | `/api/users` | 新建用户 |
| GET | `/api/groups` | 组列表 |
| POST | `/api/groups` | 新建组 |
| GET | `/api/files` | 文件列表（带权限过滤） |

### 测试环境检查清单

- [ ] 后端服务运行中（`docker compose ps` 确认）
- [ ] 数据库已初始化（Flyway 迁移已执行）
- [ ] 种子数据已导入（V3: superadmin 及初始角色）
- [ ] 前端服务运行中
- [ ] MinIO 服务运行中（文件上传下载相关用例需要）
