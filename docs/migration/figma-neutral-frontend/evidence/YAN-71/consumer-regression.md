# Consumer Regression

- `/workflow/design` now consumes Neutral Form / Settings.
- Isolated Neutral tests: `figma-neutral-m7-workflow-design.test.cjs` 2/2 PASS.
- BpmnEditor, POST `/workflow/definitions`, and process id replacement are unchanged.
- `/workflow/design/[id]`, admin, instances, templates, bindings, stats were not changed in this slice.
