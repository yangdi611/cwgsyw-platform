# Impact

GitNexus MCP was not available as a session tool. Local CLI impact on `cwgsyw-platform`:

- `TaskTemplateCreate`: LOW, 1 direct caller (`/tasks/templates/new`).
- `createTaskTemplate` payload unchanged.
