# Impact

`WikiEditorPage` is page-local. GitNexus upstream impact: 0 callers, risk LOW.
MDEditor preview engine and `createWikiMarkdownComponents` are unchanged.
APIs get/save/search/uploadAttachment are unchanged.
