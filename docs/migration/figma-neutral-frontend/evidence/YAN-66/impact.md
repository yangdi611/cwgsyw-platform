# Impact

`WikiSpaceHomePage` is page-local. GitNexus upstream impact: 0 callers, risk LOW.
Layout shell was restyled without changing `WikiTreeSidebar` behavior.
APIs `['wiki-spaces']`, `['wiki-tree', sid]`, `wikiApi.exportSpace` are unchanged.
