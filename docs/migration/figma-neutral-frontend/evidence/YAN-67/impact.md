# Impact

`WikiPageReader` is page-local. GitNexus upstream impact: 0 callers, risk LOW.
`WikiBacklinksPanel`, `WikiVersionsPanel`, and `WikiCommentsDrawer` were restyled with the same props/APIs.
`WikiMarkdown` and `WikiTreeSidebar` remain existing renderers.
