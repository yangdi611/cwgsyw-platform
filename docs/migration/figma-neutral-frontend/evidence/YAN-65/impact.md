# Impact

`WikiSpacesPage` is page-local. GitNexus upstream impact: 0 callers, risk LOW.
APIs `['wiki-spaces']` via wikiApi list/create/update/delete, `['authorization-groups']`, and wiki/group permissions are unchanged.
`ResourceAccessDialog` remains the existing authorization widget.
