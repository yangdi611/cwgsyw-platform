# Impact

`WikiGraphPage` is page-local. GitNexus upstream impact: 0 callers, risk LOW.
ReactFlow engine is unchanged. Node colors now use Neutral / status tokens.
API `['wiki-graph', sid]` GET `/wiki/spaces/:id/graph` is unchanged.
