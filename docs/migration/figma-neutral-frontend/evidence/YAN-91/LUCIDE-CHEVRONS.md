# YAN-91 leftover: Neutral chevrons

正式 Icon 集合已有 `chevron-right` / `chevron-down`，没有 `bell` / `panel-left`。

改动：

- `Breadcrumb` 分隔符从 lucide `ChevronRight` 换成 Neutral `Icon name="chevron-right"`
- `NavGroupItem` 展开箭头从 lucide `ChevronDown` 换成 Neutral `Icon name="chevron-down"`
- 通知铃铛和侧栏收起图标继续用 lucide，等待正式资产补齐

证据：`frontend/test/figma-neutral-m8-leftover-lucide-chevrons.test.cjs`
