# Impact

`ChangeDocTemplatesPage` is page-local. GitNexus upstream impact: 0 callers, risk LOW.
APIs `/admin/change-doc-templates`, upload, parse-bookmarks, active toggle, and change_doc_template read/write permissions are unchanged.
