# CMDB 逐页状态账本

## 恢复元数据

```yaml
worktree: /Users/byron/AI/worktrees/YAN-71
branch: feat/YAN-71-figma-neutral-m7-workflow-design
task: YAN-71
ready: READY_WITH_APPROVED_EXCEPTION
figmaFileKey: Z8EC6psFOj7KMfXapAFk24
figmaIconCollectionNode: "6:22411"
currentPointer: COMPLETE
currentSample: null
lastUpdatedAt: 2026-08-17
completionConfirmedBy: user
completionConfirmedAt: 2026-08-17
```

## 状态语义

| 状态 | 含义 |
| --- | --- |
| `CONFIRMED` | 已完成全部证据，且用户明确确认；默认不可重做。 |
| `IN_PROGRESS` | 当前恢复点；下一轮必须先继续此页。 |
| `TODO` | 尚未按本流程逐项审查。 |
| `BLOCKED` | 存在必须由用户或外部状态解决的阻塞；必须写明原因。 |
| `EXCLUDED` | 仅在有明确适用性结论和证据时使用，例如纯 redirect；不是跳过理由。 |

## 最终验收结论

- 2026-08-17，用户明确确认 CMDB 全模块已经收口，可全部标记完成。
- 23 个登记路由中，19 个生产页面或业务表面为 `CONFIRMED`；4 个兼容入口或内部技术页继续保持 `EXCLUDED`，不伪装为已验收的独立业务页面。
- 当前页证据日志是实施过程的时间线，其中保留的 `DEFERRED`、历史“下一指针”和未执行写操作说明仍然有效；它们不再表示有待视觉确认页面，也不会重新打开已确认页面。
- CMDB 夜间 Goal 的恢复指针为 `COMPLETE`；后续只有用户提出新的缺陷或需求时才重新进入实施流程。
- 最终本地提交门禁：完整前端测试 291/291 PASS、typecheck PASS、lint 0 error / 26 existing warnings、production build PASS；GitNexus 对最终 staged 提交包的累计变更检测为 CRITICAL（101 files / 170 symbols / 21 processes，含显式纳入的基线、Goal 文档与正式 Figma SVG），处于用户批准的 YAN-71 批次流程例外内。

## 页面顺序

| # | 路由 | 样本 / 说明 | 状态 | 最新证据或下一步 |
| ---: | --- | --- | --- | --- |
| 1 | `/cmdb` | CMDB 概览 | `CONFIRMED` | 用户已确认；基线台账已记录。 |
| 2 | `/cmdb/admin` | 模型管理 | `CONFIRMED` | 用户已确认；基线台账已记录。 |
| 3 | `/cmdb/admin/models/:modelCode` | `/cmdb/admin/models/host` | `CONFIRMED` | 编辑 Dialog 与删除 AlertDialog 已确认。 |
| 4 | `/cmdb/instances/by-model/:modelCode/:id` | `/cmdb/instances/by-model/host/24` | `CONFIRMED` | 空态图标使用 Figma `link-2`、`alert-circle`、`key` 与 `file-diff` 官方资产。实例拓扑使用与对比页一致的完整交互画布，并补齐 snake_case DTO 归一化，使模型、根节点、关键属性等基础 hover 信息一致；对比页只额外显示差异状态，用户已完成视觉确认。 |
| 5 | `/cmdb/models` | 模型入口 / 兼容页 | `EXCLUDED` | 纯服务端 redirect 到 `/cmdb/admin`，无独立视觉表面；源码、页面矩阵、YAN-88 审计、GitNexus 与真实浏览器证据一致。 |
| 6 | `/cmdb/instances` | 跨模型实例浏览 / 兼容页 | `EXCLUDED` | 纯服务端 redirect 到已确认的 `/cmdb`，无独立视觉表面；源码、页面矩阵、YAN-88 审计、GitNexus 与真实浏览器证据一致。 |
| 7 | `/cmdb/instances/by-model/:modelCode` | 使用 `host` 样本 | `CONFIRMED` | 移除内容区重复面包屑；按钮统一 32px/400；表格在 1024 保持操作列可见，390 使用双列紧凑卡片；接入服务端分页并保留 query-key 前缀兼容；定向 2/2、Neutral 270/270、typecheck、lint、build、GitNexus 与浏览器证据通过。 |
| 8 | `/cmdb/instances/by-model/:modelCode/new` | 使用 `host` 样本 | `CONFIRMED` | 移除重复面包屑和动态属性大 Card；分组改为轻量 section，1440/1024 双列、390 单列；全部控件/按钮 small/400；补齐模型加载错误态与必填校验/首错聚焦，保留原创建 payload、权限和路由。定向 2/2、Neutral 270/270、typecheck、lint、build、GitNexus 与浏览器证据通过。 |
| 9 | `/cmdb/instances/by-model/:modelCode/:id/associations` | 使用 `host/24` | `CONFIRMED` | 移除重复 Breadcrumb；查询失败与真实空态分离；空态改用 Figma `link-2` 节点 `6:27582`；方向字符改为可读“出向/入向”；筛选补可访问名称，桌面固定列宽、390 双列卡片。定向 2/2、Neutral 270/270、typecheck、lint、build 与浏览器通过；累计 unstaged change detection 升为 HIGH，已停止进入下一页。 |
| 10 | `/cmdb/instances/by-model/:modelCode/:id/associations/new` | 使用 `host/24` | `CONFIRMED` | 单 Breadcrumb；三步向导使用语义列表；定义/候选改为紧凑响应式网格；实例、定义与搜索具备 loading/error/retry，空关键词、无结果与候选状态分离；确认页与可选属性校验已验证。定向 2/2、Neutral 270/270、typecheck、lint、build、GitNexus 与 1440/1024/390 浏览器通过。未建立真实关联；自动键盘驱动未触发原生按钮默认点击、无权限身份未切换，均如实 DEFERRED。 |
| 11 | `/cmdb/associations` | 关联定义/关系入口 | `EXCLUDED` | 纯服务端 redirect 到已确认的 `/cmdb/admin`，无独立视觉、数据或交互表面；源码、迁移矩阵、YAN-88 审计、GitNexus、覆盖测试与真实浏览器证据一致。 |
| 12 | `/cmdb/impact/:instanceId` | 使用实例 `24` | `CONFIRMED` | 单 Breadcrumb；方向/深度使用 small overlay Select；补齐 hydration 与 permission 状态；根节点、层级节点与空态改为紧凑响应式结构。定向 2/2、Neutral 270/270、typecheck、lint、build 与 1440/1024/390 浏览器验证通过；真实样本无下游节点，层级折叠与无权限身份浏览器验证 DEFERRED。 |
| 13 | `/cmdb/topology/:instanceId` | 使用实例 `24` | `CONFIRMED` | 单 Breadcrumb；页头操作、DTO、筛选/详情与全屏行为保持不变。全部 `CiTopologyGraph` 使用 Token 灰色点阵；CI 节点缩至 96–132px，hover 详情使用自然高度、可停留的 184px `NodeToolbar`。筛选侧栏按 Review 收敛为 12px/16px/400，Checkbox 16×16px、行高 28px；用户已完成视觉确认。 |
| 14 | `/cmdb/topology/:instanceId/compare` | 使用实例 `24` | `CONFIRMED` | 对比条件、时间校验、DTO 与差异摘要保持不变；共享 React Flow 已统一为灰色细点背景、紧凑 CI 节点和自然高度、可停留的 184px `NodeToolbar` Popover；新增/删除/修改/未变图例局部缩至 18px 高、11px 字号。共享 symbol HIGH 已由用户明确授权，9/9 定向、typecheck 与 focused lint 通过；完整门禁继续记录，用户已完成真实视觉复查。 |
| 15 | `/cmdb/instances/2d-view` | `host` 错误态 / `rack` 数据态 | `CONFIRMED` | 单 Breadcrumb；small 可访问模型/分组 Select；查询、权限、空/错误状态分离；四/二/一列 2D 分组板与实例导航已完成。定向 2/2、Neutral 272/272、typecheck、lint、build、GitNexus 与真实浏览器通过；用户已确认。 |
| 16 | `/cmdb/spatial` | 活动 1 条 / 归档 28 条 | `CONFIRMED` | 单 Breadcrumb；活动/归档筛选、搜索与清除、四/二/一列布局卡、真实状态/详情导航、新建 Dialog 与归档 AlertDialog 已验证。定向 4/4、Neutral 272/272、typecheck、lint、build、GitNexus 与浏览器通过；用户已确认。 |
| 17 | `/cmdb/spatial/rooms/:roomId` | `/cmdb/spatial/rooms/332` | `CONFIRMED` | 单 Breadcrumb；Detail/Drawer 页面结构、查询/权限状态、搜索与图层工具栏、520/440/360px 画布、缩放和选中详情已验证；用户已确认。 |
| 18 | `/cmdb/spatial/rooms/:roomId/edit` | `/cmdb/spatial/rooms/332/edit` | `CONFIRMED` | 入口与草稿查询状态、紧凑编辑器头部、三栏桌面、1024 双列下置面板和 390 单列完整编辑区已验证；用户已确认。 |
| 19 | `/cmdb/spatial/rooms/:roomId/versions` | `/cmdb/spatial/rooms/332/versions` | `CONFIRMED` | 单 Breadcrumb；完整入口/查询状态、已发布状态、紧凑桌面表格、390 两列版本卡片与恢复确认取消流程已验证；用户已确认。 |
| 20 | `/cmdb/spatial/spike` | 内部 500 节点 Canvas 技术验证 | `EXCLUDED` | PAGE-MIGRATION-MATRIX 明确“spike 仅内部”；合成数据、无 API/RBAC/写入、无业务导航入口，GitNexus 与浏览器证据一致，不属于生产逐页视觉验收面。 |
| 21 | `/cmdb/changes` | `http://localhost/cmdb/changes` | `CONFIRMED` | 单 Breadcrumb；筛选具备可访问名称和稳定五/二/一列重排；模型与主查询 error/retry、权限、日期范围、真实空态和筛选无结果均可辨识；桌面紧凑表格、390 双列语义按钮卡片、分页和刷新状态已验证。定向 3/3、Neutral 279/279、typecheck、lint、build、GitNexus 与真实浏览器通过；用户已确认。 |
| 22 | `/cmdb/changes/stats` | `http://localhost/cmdb/changes/stats` | `CONFIRMED` | 单 Breadcrumb；日期范围、指标层级、每日趋势、Top 10 与 loading/empty/error/permission 状态已收敛；1440/1024/390 响应式和返回导航已验证，用户已确认。 |
| 23 | `/cmdb/alerts` | `http://localhost/cmdb/alerts` | `CONFIRMED` | 单 Breadcrumb；级别/状态筛选具备可访问名称与两列/单列重排；loading、permission、query error/retry、真实空态和筛选无结果可辨识；桌面固定表格与 390 双列告警卡、真实状态色、确认状态、分页和刷新反馈已收敛。空态使用 Figma `alert-circle` 节点 `6:22984`。最终告警/概览/实例详情回归 13/13、Neutral 282/282、typecheck、lint、build、GitNexus 与真实零数据浏览器检查通过；用户已确认。 |

## 当前页证据日志

> 以下为实施过程时间线。行内历史恢复指针不覆盖本文件顶部的最终 `currentPointer: COMPLETE`。

| 日期 | 路由 | 证据 | 结果 |
| --- | --- | --- | --- |
| 2026-08-17 | `CMDB FINAL ACCEPTANCE` | 用户完成逐页人工视觉 Review，并明确确认“CMDB 的所有模块全部收口，可以 mark 全部完成”。 | 19 个生产页面/业务表面统一标记 `CONFIRMED`；4 个兼容或内部入口保留 `EXCLUDED`；无剩余 `TODO`、`IN_PROGRESS`、待确认队列或 `BLOCKED` 页面。 |
| 2026-08-17 | `/cmdb/instances/by-model/host/24` | Figma 图标集合 `6:22411` 中定位 `file-key-2`；具体节点 `6:25867` 已调用 design context；资产保存为 `frontend/public/figma-icons/cmdb-resource-file-key-2.svg`。 | 节点与耐久资产证据完成；尚未获得用户整页确认。 |
| 2026-08-17 | `/cmdb/instances/by-model/host/24` | GitNexus MCP 未挂载，已回退项目 CLI；索引为最新。`InstanceResourcesTab` 影响 2 个上游符号，`Section` 影响 3 个，均为 `LOW`，只进入实例详情页模块链。 | 允许在批准范围内继续；完成后仍需 `detect-changes` 与回归证据。 |
| 2026-08-17 | `/cmdb/instances/by-model/host/24` | 定向测试 2/2；全套 Neutral 测试 270/270；typecheck PASS；lint 0 error / 31 条既有 warning；浏览器确认官方资产唯一出现、可见且实测 24×24；最终 unstaged `detect-changes` 为 MEDIUM（包含本工作树此前未提交改动）。 | `CONFIRMED`；下一指针 `/cmdb/models`。 |
| 2026-08-17 | `/cmdb/models` | 源码仅调用 `redirect('/cmdb/admin')`；PAGE-MIGRATION-MATRIX 与 YAN-88 page audit 已列为 approved redirect；GitNexus CLI 为 LOW、0 上游/0 流程；浏览器从 `/cmdb/models` 最终进入 `/cmdb/admin` 并显示“模型管理”，无 console error；page coverage 1/1 PASS。 | `EXCLUDED`，无独立视觉表面、无需 Figma 或代码修改；下一指针 `/cmdb/instances`。 |
| 2026-08-17 | `/cmdb/instances` | 源码仅调用 `redirect('/cmdb')`；PAGE-MIGRATION-MATRIX 与 YAN-88 page audit 已列为 approved redirect；GitNexus CLI 为 LOW、0 上游/0 流程；浏览器最终进入 `/cmdb`，显示已确认的 CMDB 概览及实例浏览，无 console error；page coverage 1/1 PASS。 | `EXCLUDED`，无独立视觉表面、无需 Figma 或代码修改；下一指针 `/cmdb/instances/by-model/:modelCode`，样本 `host`。 |
| 2026-08-17 | `/cmdb/instances/by-model/host` | GitNexus CLI：`InstanceListPage` LOW、0 上游、0 流程；页面移除重复 Breadcrumb，按钮收敛为 32px/400，桌面固定表格防止操作列裁切，390 卡片改为双列紧凑网格，并使用 `page=1` / `size=20` 的既有后端合同接入共享 Pagination。定向测试 2/2、Neutral 270/270、typecheck PASS、lint 0 error/31 existing warnings、build PASS、`git diff --check` PASS；unstaged change detection 为 MEDIUM，来自同一脏工作树 13 文件/17 symbols，当前页只关联 `InstanceListPage → PageShell`。浏览器在 1440/1024/390 均无横向溢出且仅一个面包屑；1024 操作列 112px 且按钮可见；390 为 6 张双列卡片；行点击打开 Drawer，选择按钮不打开 Drawer并显示批量编辑；console 0 error/warning。 | `CONFIRMED`；真实样本仅 6 条，分页控件的多页状态由 `total: 45` fixture 锁定；下一指针 `/cmdb/instances/by-model/:modelCode/new`，样本 `host/new`。 |
| 2026-08-17 | `/cmdb/instances/by-model/host/new` | GitNexus CLI：`NewInstancePage` LOW、0 上游/0 流程；`renderField` LOW、1 个直接消费者且仅进入本页 3 条流程。页面移除重复 Breadcrumb 和动态属性 Card，改为轻量语义 section；桌面双列、390 单列，全部字段与操作使用 small 密度；模型查询失败展示可重试 ErrorState；空提交显示实例名称、序列号、内网 IP 三项错误并聚焦首错；Select overlay 实测 521px client/scroll width 一致、选中即关闭；取消返回实例列表。定向 2/2、Neutral 270/270、稳定态 typecheck PASS、lint 0 error/31 existing warnings、build PASS、`git diff --check` PASS；unstaged detect-changes 为 MEDIUM，当前页映射到 NewInstancePage 的 PageShell/Spinner/Skeleton 三条流程。浏览器 1440/1024 双列、390 单列且按钮等宽，三视口仅一套面包屑、无横向溢出、console 0 error/warning。 | `CONFIRMED`；没有新增/替换图标，因此 Figma SVG 协议 Not applicable；创建成功未执行，避免生成测试数据，原 payload 由源码和结构测试锁定。下一指针 `/cmdb/instances/by-model/:modelCode/:id/associations`，样本 `host/24`。 |
| 2026-08-17 | `/cmdb/instances/by-model/host/24/associations` | Figma 集合 `6:22411` 中比较 `link-2`（`6:27582`）、`network`（`6:28340`）与 `git-branch`（`6:26740`），选择语义最直接的 `link-2`；三者均读取 design context，原始 SVG 保存为 `frontend/public/figma-icons/cmdb-association-link-2.svg`。GitNexus 预编辑 impact：`AssociationsPage` LOW、0 上游/0 流程。页面移除重复 Breadcrumb，查询错误不再吞成空数组，筛选使用 FilterBar 并补 `aria-label`，方向从箭头字符改为“出向/入向”，表格列宽与 390 双列卡片受局部作用域约束。定向 2/2、Neutral 270/270、稳定态 typecheck PASS、lint 0 error/31 existing warnings、build PASS、`git diff --check` PASS。浏览器 1440/1024/390 均一个 Breadcrumb、无横向溢出、官方图标 24×24 且无 inbox glyph、console 0 error/warning；新建入口导航与返回 PASS。样本为 0 条关联，真实 Drawer/删除 AlertDialog 交互 DEFERRED，未制造或删除数据，结构和 API 由 fixture 锁定。 | `CONFIRMED`；最终全工作树 detect-changes 为 HIGH（17 files/29 symbols/8 flows），本页只涉及 `AssociationsPage` 的 UseBreadcrumbStore/SetDynamicLabels/PageShell 3 条流程。按停机规则不进入下一页；恢复需用户决定先 commit 固化当前批次或批准累计 HIGH 例外。 |
| 2026-08-17 | `GOAL` | 用户更新当前 Goal：累计 worktree HIGH 以及设计冲突、破坏性选择、权限/数据阻塞、范围扩大均记录到文档后继续队列，不再作为自动停机条件；不安全或不可验证状态仍必须 DEFERRED/BLOCKED，禁止猜测或执行未授权动作。完整边界写入 `evidence/YAN-71/PROCESS-EXCEPTION.md`。 | 夜间队列恢复；commit/push/PR/merge/deploy/tracker 仍为 `NOT AUTHORIZED`。 |
| 2026-08-17 | `/cmdb/instances/by-model/host/24/associations/new` | GitNexus CLI 索引最新；`NewAssociationPage` 为 LOW、0 上游/0 受影响流程。页面移除重复 Breadcrumb；向导步骤使用 `ol/li` 与 `aria-current=step`；定义描述改为“源模型/目标模型”而非箭头字符；查询失败不再吞掉，搜索仅在非空关键词时启用并区分初始、loading、error/retry、无结果与候选；确认属性改为原生 `fieldset`，空属性名显示 `role=alert`，避免把 `error` 属性透传给 DOM。未新增或替换图标，Figma SVG 协议 Not applicable。 | 页面局部实现完成，API、query key、RBAC、数据模型、route 与创建 payload 保持不变。 |
| 2026-08-17 | `/cmdb/instances/by-model/host/24/associations/new` | 定向 2/2、Neutral 270/270、typecheck PASS、lint 0 error/31 existing warnings、build PASS、`git diff --check` PASS。1440/1024/390 均 1 套 Breadcrumb、无横向溢出；1440 定义卡 3×356px，390 单列 334px；搜索初始/无结果/候选、候选选中、确认、属性错误、两次上一步状态恢复与取消导航 PASS；校验修复后新增 console error/warning 为 0。最终累计 unstaged detect-changes 为 HIGH（19 files/33 symbols/11 flows），本页仅进入 NewAssociationPage 的 UseBreadcrumbStore/SetDynamicLabels/PageShell 3 条流程。 | `CONFIRMED`；未点击“建立关联”，避免创建数据。自动化键盘驱动虽能将焦点移动到原生 Card `<button>`，但未触发浏览器默认点击，键盘激活记为 `DEFERRED`；无权限身份状态同样 `DEFERRED`。下一指针 `/cmdb/associations`。 |
| 2026-08-17 | `/cmdb/associations` | 源码仅调用 `redirect('/cmdb/admin')`；`PAGE-MIGRATION-MATRIX.md` 与 YAN-88 page audit 均列为 approved redirect；GitNexus CLI `OldAssociationsPage` 为 LOW、0 上游/0 流程；真实浏览器最终进入 `/cmdb/admin`，显示已确认的“模型管理”、单 Breadcrumb、console 0 error/warning；page coverage 2/2 PASS；`git diff --check` PASS。 | `EXCLUDED`，无独立视觉/数据/权限/响应式表面，无代码或 SVG 修改；typecheck/lint/build 为 Not applicable/NOT RUN。累计 unstaged detect-changes 仍为批准例外内的 HIGH（19 files/33 symbols/11 flows）。下一指针 `/cmdb/impact/:instanceId`，样本 `24`。 |
| 2026-08-17 | `/cmdb/impact/24` | GitNexus CLI 索引最新；预编辑 `ImpactAnalysisPage` 为 LOW、0 上游/0 受影响流程。页面移除重复 Breadcrumb；方向与深度 Select 增加可访问名称并保持 overlay；补齐 hydration loading 与 permission error；根节点、层级 section、节点 Card 网格及无下游节点空态统一为 12px/400 的轻量响应式结构。保留 query key、POST payload、retry、影响边归组算法、RBAC 与详情导航；没有新增或替换图标，Figma SVG 协议 Not applicable。定向 2/2、Neutral 270/270、typecheck PASS、lint 0 error/31 existing warnings、build PASS、`git diff --check` PASS。浏览器 1440/1024/390 均单 Breadcrumb、无横向溢出；两个 Select overlay 无横向滚动、选择后关闭并重新查询；返回实例导航 PASS；console 0 error/warning。 | `CONFIRMED`；最终累计 unstaged detect-changes 为 HIGH（21 files/41 symbols/13 flows），本页只新增 `ImpactAnalysisPage → IsAxiosError` 与 `ImpactAnalysisPage → PageShell` 两条流程，处于已批准例外内。真实样本只有根节点，真实层级折叠和无权限身份浏览器验证 `DEFERRED`，层级结构由 fixture 锁定。下一指针 `/cmdb/topology/:instanceId`，样本 `24`。 |
| 2026-08-17 | `/cmdb/topology/24` | GitNexus CLI 索引最新；预编辑 `TopologyPage` LOW、0 上游/0 流程。`CiTopologyGraph` 为 HIGH：3 个直接消费者、5 个上游，影响实例详情、拓扑对比和当前页，因此未修改该共享 symbol。页面移除重复 Breadcrumb；页头按钮统一 32px/400；后端 `model_id/model_name/model_color/is_root/key_attrs` 在页内适配为图谱合同，恢复真实根节点标题、返回实例和模型筛选；查询错误不再吞为空态；补齐 hydration/permission/error retry；画布使用 520/440/360px 稳定高度；筛选与节点详情改为轻量语义 section；加入文本式全屏、退出和 Escape 清理。没有新增/替换图标，Figma SVG 协议 Not applicable。定向 3/3、Neutral 271/271、typecheck PASS、lint 0 error/28 existing warnings（本页消除 3 条 hook warning）、build PASS、`git diff --check` PASS。浏览器 1440/1024/390 均单 Breadcrumb、无横向溢出；画布分别 520/440/360px；节点详情、模型/状态筛选、全屏进入与 Escape 退出 PASS；新标签 console 0 error/warning。 | `CONFIRMED`；累计 unstaged detect-changes HIGH（23 files/55 symbols/14 flows），本页只新增 `TopologyPage → PageShell`。共享图谱节点与 hover Tooltip 的旧字号/粗体、真实多节点折叠，以及无权限身份浏览器验证 `DEFERRED`；共享图谱内部结构由既有实现保留，未越过直接 symbol HIGH 授权。Documentation Impact Assessment：PRD/ADR/API/数据/迁移/安全/运维文档均 Not applicable（产品范围与稳定合同未变）；Current System Baseline `Update`（本行及 CMDB 基线）；后续文档任务 Not applicable，技术遗留已在流程例外中记录。下一指针 `/cmdb/topology/:instanceId/compare`，样本 `24`。 |
| 2026-08-17 | `/cmdb/topology/24/compare` | GitNexus CLI 索引最新；`TopologyComparePage` LOW、0 上游/0 流程，`mergeCompare` LOW、1 个直接消费者/0 流程。页面移除重复 Breadcrumb；返回按钮与开始对比统一 32px/400；起止时间和深度改为语义 Field 网格；时间倒置显示 `role=alert` 并禁用提交；补齐 hydration、permission、loading、error/retry 与未对比/无节点空态；后端 `model_id/model_name/model_color/is_root/key_attrs` 在 `mergeCompare` 内适配；差异摘要、四类真实状态图例和独立画布使用轻量 section。没有新增/替换图标，Figma SVG 协议 Not applicable；共享 `CiTopologyGraph` 沿用已记录的直接 symbol HIGH DEFERRED，未修改其内部节点、Tooltip 或布局算法。定向 3/3、Neutral 272/272、typecheck PASS、lint 0 error/28 existing warnings、build PASS、`git diff --check` PASS。真实浏览器时间倒置校验 PASS；使用 `2025-01-01 00:00` 至 `2026-08-17 23:59` 的只读 GET 对比返回新增 1、删除 10、修改 0、未变 0，根节点标题和模型字段正确；1440/1024/390 均单 Breadcrumb、无横向溢出，画布分别 520/440/360px；新标签 console 0 error/warning。 | `CONFIRMED`；累计 unstaged detect-changes HIGH（25 files/65 symbols/15 flows），本页只增加 `TopologyComparePage → PageShell`。没有写入或修改业务数据。无权限身份与强制网络错误的浏览器验证 `DEFERRED`，结构由页面状态与 fixture 锁定。Documentation Impact Assessment：PRD/ADR/API/数据/迁移/安全/运维文档均 Not applicable（范围和稳定合同未变）；Current System Baseline `Update`（本行及 CMDB 基线）；后续文档任务 Not applicable，共享图谱遗留已在现有流程例外中记录。下一指针 `/cmdb/instances/2d-view`。 |
| 2026-08-17 | `/cmdb/instances/2d-view` | GitNexus CLI 索引最新；预编辑 `TwoDViewPage` LOW、0 上游/0 流程，`statusMeta` LOW、1 个直接消费者且仅进入 `TwoDViewPage → PageShell`。页面移除重复 Breadcrumb；模型和分组 Select 使用 32px、overlay 与可访问名称；模型、属性、视图查询不再吞错，补齐 hydration、permission、loading、empty、error/retry；默认分组字段在控件中同步显示。分组结果从 Card/StackList 嵌套改为四/二/一列的轻量语义 section，实例行使用 12px/400、状态 Badge、hover/focus 与详情链接；`online` 显示为“在线”。无新增/替换 SVG，Figma 图标协议 Not applicable。定向 2/2、Neutral 272/272、typecheck PASS、lint 0 error/28 existing warnings、build PASS、`git diff --check` PASS。浏览器：默认选择态、`host` 真实“未启用 2D 视图”错误态、`rack` 真实 1 组/1 实例数据态、分组字段切换、刷新 toast、Select overlay 与 1440/1024/390 均 PASS；三档无横向溢出、单 Breadcrumb、console 0 error/warning。 | `CONFIRMED`；累计 unstaged detect-changes 为 CRITICAL（28 files/72 symbols/16 flows），但本页只新增 `TwoDViewPage → PageShell`，累计升级已写入流程例外且任何交付动作仍禁止。无权限身份、模型/属性强制网络错误的真实浏览器验证 `DEFERRED`，结构由状态分支和 fixture 锁定。Documentation Impact Assessment：PRD/ADR/API/数据/迁移/安全/运维文档均 Not applicable（产品范围与稳定合同未变）；Current System Baseline `Update`（本行及 CMDB 基线）；后续文档任务 Not applicable。下一指针 `/cmdb/spatial`。 |
| 2026-08-17 | `/cmdb/spatial` | GitNexus CLI 索引最新；预编辑 `SpatialLayoutPage` LOW、0 上游/0 流程，`SpatialLayoutIndex` LOW、1 个直接消费者/0 流程。页面移除重复 Breadcrumb；补齐 hydration 与 permission 状态；活动/归档筛选组、搜索框和清除行为具备可访问名称；布局加载错误可重试，搜索无结果与真实空态分离；房间列表补 loading/error/retry，创建/归档/恢复错误保留在对应 Dialog。布局卡改为四/二/一列，标题 13px/500、正文 12px/400，状态、更新时间、详情导航和 small 操作完整；创建成功导航改用等价 `router.push`，API 与 query key 不变。无新增/替换 SVG，Figma 图标协议 Not applicable。定向/受影响 4/4、Neutral 272/272、typecheck PASS、lint 0 error/27 existing warnings、build PASS、`git diff --check` PASS。浏览器：真实活动 1 条、归档 28 条、搜索无结果与清除、详情 href、新建 Dialog 390px 边界、归档 AlertDialog 打开/取消均 PASS；1440/1024/390 分别四/二/一列且无横向溢出、单 Breadcrumb、console 0 error/warning。 | `CONFIRMED`；累计 unstaged detect-changes CRITICAL（31 files/77 symbols/16 flows），流程数未增加，当前两个 symbol 均 LOW。未创建、归档或恢复任何布局；无权限身份与强制网络错误浏览器验证 `DEFERRED`，结构由状态分支和 fixture 锁定。Documentation Impact Assessment：PRD/ADR/API/数据/迁移/安全/运维文档均 Not applicable（产品范围和稳定合同未变）；Current System Baseline `Update`（本行及 CMDB 基线）；后续文档任务 Not applicable。下一指针 `/cmdb/spatial/rooms/:roomId`，样本 `332`。 |
| 2026-08-17 | `/cmdb/spatial/rooms/332` | GitNexus CLI 索引最新；预编辑 `SpatialRoomPage` LOW、0 上游/0 流程，`SpatialRoomViewer` LOW、1 个直接消费者/0 流程，`SpatialViewerCanvas` LOW、1 个直接消费者且两层总计 2 个上游/0 流程。入口不再在 hydration、权限或无效参数时返回空白；已发布查看器改用单一 Detail/Drawer Pattern 与紧凑 Page Header，返回/版本/编辑操作和搜索/图层工具栏分层。已发布版本、运行数据和空间定位查询分别呈现 loading、empty、error/retry；搜索具有可访问名称、Enter 提交与清除。画布移除 Tailwind 与行内定位，使用局部 520/440/360px 配方；缩放组具备可访问名称，详情栏在 1024/390 自动下移。无新增/替换 SVG，Figma 图标协议 Not applicable。定向 4/4、相关空间 6/6、Neutral 274/274、typecheck PASS、lint 0 error/27 existing warnings、build PASS、`git diff --check` PASS。真实浏览器：已发布版本 5、搜索无结果/清除、状态图层、放大 46%→56%、选择 `N-13` 机柜位及详情均 PASS；1440/1024/390 均单 Breadcrumb、无横向溢出，画布分别 826×520、659×440、317×360，console 0 error/warning。 | `CONFIRMED`；累计 unstaged detect-changes CRITICAL（34 files/90 symbols/16 flows），当前三个 symbol 均 LOW，处于批准例外内。真实无权限身份、强制发布/运行/定位网络错误与真实部分运行数据浏览器验证 `DEFERRED`，状态由页面分支和定向结构测试锁定。Documentation Impact Assessment：PRD/需求、ADR、API/数据模型/迁移/安全/运维文档均 Not applicable（产品范围、稳定合同和运行边界未变）；Current System Baseline `Update`（本行、CMDB 基线与页面对照表）；后续文档任务 Not applicable。下一指针 `/cmdb/spatial/rooms/:roomId/edit`，样本 `332`。 |
| 2026-08-17 | `/cmdb/spatial/rooms/332/edit` | GitNexus CLI 索引最新；预编辑 `SpatialEditPage` LOW、0 上游/0 流程，`SpatialEditor` LOW、1 个直接消费者/0 流程，`SpatialEditorSession` LOW、1 个直接消费者且两层共 2 个上游/0 流程。路由入口补齐 hydration、permission、无效参数、布局列表 loading/error/retry 与无活动布局状态；草稿查询补齐 loading/error/retry/empty。编辑器根布局移除负边距/固定视口 Tailwind 组合，使用局部 Neutral 配方：1440 保持 208px 组件库 + 弹性画布 + 288px 属性栏，面板可独立滚动；1024 画布从原 213px 扩为 710×440，两个面板移到画布下方双列；390 为 368×360 画布和下方单列组件库/属性栏，功能不再隐藏。标题、状态和操作统一 12–13px/400。无新增/替换 SVG，Figma 图标协议 Not applicable。定向/相关 8/8、Neutral 276/276、typecheck PASS、lint 0 error/27 existing warnings、build PASS、`git diff --check` PASS。真实浏览器：1440 三栏 208/645/288px；1024 画布 710×440；390 画布 368×360 且组件库/属性栏可见；放大 36%→46%、适配到 19%、只读选择“禁止区”并显示属性均 PASS；三档单 Breadcrumb、无横向溢出，console 0 error/warning。 | `CONFIRMED`；累计 unstaged detect-changes CRITICAL（36 files/98 symbols/16 flows），当前三个 symbol 均 LOW。没有点击校验、保存或发布，构建后状态仍为“已保存”，未修改草稿或发布版本。真实无权限身份、强制布局/草稿网络错误、冲突保存、导入确认、校验和发布 Dialog 浏览器验证 `DEFERRED`；结构与原合同由定向及既有 overlay/input 测试锁定。Documentation Impact Assessment：PRD/需求、ADR、API/数据模型/迁移/安全/运维文档均 Not applicable（产品范围、稳定合同和写入协议未变）；Current System Baseline `Update`（本行、CMDB 基线与页面对照表）；后续文档任务 Not applicable。下一指针 `/cmdb/spatial/rooms/:roomId/versions`，样本 `332`。 |
| 2026-08-17 | `/cmdb/spatial/rooms/332/versions` | GitNexus CLI 索引最新；预编辑 `SpatialVersionsPage` LOW、0 上游/0 流程，`SpatialVersionHistory` LOW、1 个直接消费者/0 流程。入口补齐 hydration、permission 与无效参数状态；布局和版本查询分别呈现 loading、empty、error/retry。移除内容区重复 Breadcrumb；返回查看器使用等价 `router.push`，消除 `window.location.href` lint warning。真实 `version.state === PUBLISHED` 显示 success StatusBadge；桌面表格使用 compact 密度和 760px 最小宽度，避免 `max-content + fixed` 产生异常宽度；390 版本卡以版本/操作整行、其余四项两列呈现。恢复失败 Alert 进入页内内容，恢复进行中阻止误关闭与重复操作。无新增/替换 SVG，Figma 图标协议 Not applicable。相关空间 10/10、Neutral 278/278、typecheck PASS、lint 0 error/26 existing warnings、build PASS、`git diff --check` PASS。真实浏览器：5 条已发布版本；1440 表格 1090px，1024 表格/容器 760px，390 为 5 张卡片且中间四项各 141px 双列；三档单 Breadcrumb、无页面横向溢出。恢复 AlertDialog 打开后取消并正确卸载，console 0 error/warning。 | `CONFIRMED`；累计 unstaged detect-changes CRITICAL（38 files/103 symbols/16 flows），当前两个 symbol 均 LOW。没有确认恢复、没有创建草稿或发布版本。真实无权限身份、强制布局/版本网络错误、无版本数据、恢复失败和进行中状态浏览器验证 `DEFERRED`，结构由状态分支和定向测试锁定。Documentation Impact Assessment：PRD/需求、ADR、API/数据模型/迁移/安全/运维文档均 Not applicable（产品范围、稳定合同和恢复协议未变）；Current System Baseline `Update`（本行、CMDB 基线与页面对照表）；后续文档任务 Not applicable。下一指针 `/cmdb/spatial/spike`。 |
| 2026-08-17 | `/cmdb/spatial/spike` | `PAGE-MIGRATION-MATRIX.md` 明确标记 `spike 仅内部`；源码固定生成 `SLOT_COUNT = 500` 的合成机柜位，只依赖 Konva 与本地状态，无空间 API、query key、权限判断、业务数据、保存或发布路径。全仓引用仅路由、Breadcrumb 配置和两份测试，Sidebar 与业务页面没有入口。GitNexus CLI：`SpatialCanvasSpikePage` LOW、0 上游/0 流程；`SpatialCanvasSpike` LOW、1 个直接消费者/0 流程。真实浏览器直链可达，标题“空间画布技术验证”，显示 500 个机柜位说明；1440/390 均单 Breadcrumb、无横向溢出、console 0 error/warning；缩放 65%→75% 与重置 65% PASS。相关空间测试 10/10、`git diff --check` PASS；本切片无代码/CSS/SVG 修改，typecheck/lint/build 复用紧邻版本页的未变 PASS 证据，未重复运行。 | `EXCLUDED`；结论仅排除其生产逐页视觉验收适用性，不删除路由。残余风险：内部试验页仍可通过已登录 App Shell 的直链访问，且没有专用权限门禁；删除、环境隔离或新增权限属于产品/安全范围扩大，记录为 `Follow-up task`，但创建 Linear 任务未获授权。Documentation Impact Assessment：PRD/需求 Not applicable（内部合成样例不定义产品行为）；ADR/API/数据/迁移/安全/运维文档 Not applicable（无合同变更）；Current System Baseline `Update`（本行、CMDB 基线与页面对照表）；后续文档任务 `Follow-up task`（决定直链保留、移除或门禁）。累计 unstaged detect-changes 仍为 CRITICAL（38 files/103 symbols/16 flows）。下一指针 `/cmdb/changes`。 |
| 2026-08-17 | `/cmdb/changes` | GitNexus CLI 索引最新；预编辑 `CmdbChangesPage` LOW、0 上游/0 流程，`actionTone` 与 `toIso` 均 LOW、各 1 个直接消费者。页面移除内容区重复 Breadcrumb；全部筛选控件补可访问名称，模型查询不再吞错，模型/变更查询分别具备 error/retry，补齐 hydration、permission、日期倒置、真实空态和筛选无结果状态。筛选在 1440/1024/390 使用五/二/一列重排；桌面表格固定关键列，390 使用共享 Button 承载双列信息卡，保留分页、筛选参数、query key、RBAC 与只读 GET 合同。无新增/替换 SVG，Figma 图标协议 Not applicable。定向 3/3、相关 9/9、Neutral 279/279、typecheck PASS、lint 0 error/26 existing warnings、build PASS、`git diff --check` PASS。真实浏览器：1440/1024/390 均单 Breadcrumb、无横向溢出；1440 五列、1024 两列、390 单列筛选和 20 张双列卡片；分页 1→2、搜索无结果/清除、Select overlay 302px client/scroll width 相等均 PASS；console 0 error/warning。 | `CONFIRMED`；累计 unstaged detect-changes CRITICAL（40 files/106 symbols/17 flows），本页只新增 `CmdbChangesPage → PageShell`。真实前两页均无 `beforeJson/afterJson`，变更对比展开、强制查询错误和无权限身份浏览器验证 `DEFERRED`，状态由页面分支与定向测试锁定。Documentation Impact Assessment：PRD/需求、ADR、API/数据模型/迁移/安全/运维文档均 Not applicable（范围和稳定合同未变）；Current System Baseline `Update`（本行、CMDB 基线与页面对照表）；后续文档任务 `Follow-up task`（仓库规则引用但缺失的 `docs/standards/code-review-checklist.md` 需恢复或纠正路径，Linear mutation 未授权）。下一指针 `/cmdb/changes/stats`。 |
| 2026-08-17 | `/cmdb/changes/stats` | GitNexus CLI 索引最新；预编辑 `CmdbChangesStatsPage`、`ActionCountCard`、`DailyBarChart`、`toIso`、查询函数与测试辅助函数均为 LOW，`DailyBarChart` 仅有一个直接消费者并进入 `CmdbChangesStatsPage → PageShell`。页面移除内容区重复 Breadcrumb；日期范围移到指标之前并补充语义名称；日期倒置停止查询并显示错误；补齐 hydration、permission、query error/retry；指标值收敛为 18px/500，标签与说明为 12px/400；每日趋势使用有 table/progress 语义的 Neutral/success/danger 图表，Top 10 缺失模型回退为 `modelId` 或 `—`；反馈区在 1440 双列、1024/390 单列，390 Top 10 使用双列紧凑卡片。没有新增/替换 SVG，Figma 图标协议 Not applicable。定向 3/3、相关 12/12、Neutral 280/280、typecheck PASS、lint 0 error/26 existing warnings、生产 build PASS、`git diff --check` PASS。真实浏览器：1440/1024/390 均单 Breadcrumb、无横向溢出；指标卡分别 3×351px、3×212px、单列 319px；1440 趋势与 Top 10 双列，1024/390 单列；390 Top 10 为 10 张双列紧凑卡；真实今日/本周/本月均 3、趋势 7 条、Top 10 为 10 条，缺少模型的已删除实例显示 `—`；返回变更历史及浏览器返回 PASS；console 0 error/warning。 | `CONFIRMED`；最终累计 unstaged detect-changes CRITICAL（42 files/109 symbols/18 flows），统计页自身只进入 `CmdbChangesStatsPage → PageShell`，属于已批准累计例外。Browser 的 fill/type 无法对原生 `type=date` 触发 React change，因此真实范围筛选、清除和日期倒置为 `DEFERRED`；强制 query error 与无权限身份同为 `DEFERRED`，源码分支和定向测试已覆盖且未伪造数据。Documentation Impact Assessment：PRD/需求、ADR、API/数据模型/迁移/安全/运维文档均 Not applicable（产品范围和稳定合同未变）；Current System Baseline `Update`（本行、CMDB 基线与页面对照表）；后续文档任务 `Follow-up task`（缺失的 `docs/standards/code-review-checklist.md` 路径仍需恢复或纠正，Linear mutation 未授权）。下一指针 `/cmdb/alerts`。 |
| 2026-08-17 | `/cmdb/alerts` | GitNexus MCP 未加载，使用项目 CLI；索引与 `e299e58` 同步。预编辑 `CmdbAlertsPage`、`severityMeta`、`statusMeta` 及测试辅助函数均为 LOW；两个 meta helper 各只有当前页一个直接消费者。页面移除内容区重复 Breadcrumb；补齐 hydration、permission、query error/retry 和明确 loading；级别/状态 Select 增加可访问名称，在 1440/1024 双列、390 单列；真实无数据与筛选无结果分离并可清除。`info` 使用真实 info tone，critical/warning/firing/resolved 保留 danger/warning/danger/success；桌面使用 920px 固定表格与局部横向滚动，390 使用双列紧凑告警卡；确认按钮具备对象化可访问名称，已确认状态使用 Neutral Badge；分页只在多页时显示，并保留后台刷新反馈。Figma 正式集合 `6:22411` 中选择 `alert-circle` 节点 `6:22984`，原始资产保存为 `frontend/public/figma-icons/cmdb-alert-circle.svg`，SHA-256 `d0ee4645f1a8e9710b5d0903f7c3bf8f96aebc60122e205b49659a6cd490fd8a`，与下载字节一致。最终告警/概览/实例详情回归 13/13、Neutral 282/282、typecheck PASS、lint 0 error/26 existing warnings、production build PASS、`git diff --check` PASS。真实浏览器当前接口为 0 条告警：1440/1024/390 均单 Breadcrumb、无横向溢出；筛选分别两列/两列/单列，Select overlay 无横向滚动，选择“警告”后出现筛选无结果及清除操作，清除后恢复真实空态；官方图标 22×22，空态标题 13px/400、说明 12px/400；最终新标签 console 0 error/warning。 | `CONFIRMED`；累计 unstaged detect-changes CRITICAL（44 files/114 symbols/19 flows），本页只新增 `CmdbAlertsPage → PageShell`，处于已批准累计例外内。真实桌面数据表、390 数据卡、状态 Badge、分页和确认操作因接口零数据且禁止制造/确认告警而 `DEFERRED`；强制 query error、无权限身份与 Dark 浏览器状态同为 `DEFERRED`，结构由页面分支和定向测试锁定。Documentation Impact Assessment：PRD/需求、ADR、API/数据模型/迁移/安全/运维文档均 Not applicable（产品范围和稳定合同未变）；Current System Baseline `Update`（本行、CMDB 基线与页面对照表）；后续文档任务 `Follow-up task`（缺失的 `docs/standards/code-review-checklist.md` 需恢复或纠正路径，Linear mutation 未授权）。无剩余 `TODO`/`IN_PROGRESS`，最终指针 `COMPLETE`。 |
| 2026-08-17 | 共享 `CiTopologyGraph` | 用户明确要求所有 React Flow 消费者统一修改。精确 GitNexus UID 为 HIGH：3 个直接消费者、5 个上游、3 条相关流程；范围为普通拓扑、拓扑对比及实例详情拓扑 Tab。画布使用系统 Token 的灰色面与 18px/1px 深灰点；用户第二轮截图证明单节点仍被 fitView 放大，因此初始 `maxZoom` 限制为 1、交互上限 1.5。CI 节点缩至 96–132px、4×6px padding，取消根节点第二圈描边。用户第三轮要求对齐 Neutral Card 后，节点主体统一为 surface/subtle border/6px radius/no resting shadow；差异色只作为左侧 2px 状态条和轻量标签，hover 才使用 surface-hover/elevation-sm。模型为无框次级文字；hover 详情保持 220px 系统 `cwgsyw-popover`，controls 28px、minimap 120×72。布局、折叠、点击、边算法、props、API/query key/RBAC/路由均未变。定向 9/9、Neutral 283/283、typecheck PASS、lint 0 error/26 existing warnings、production build PASS、`git diff --check` PASS。 | `CONFIRMED`；真实三入口视觉由用户当前已登录页面继续确认。累计 unstaged detect-changes CRITICAL（45 files/127 symbols/19 flows），共享 HIGH 为本次用户明确授权。 |
| 2026-08-17 | `/cmdb/instances/by-model/host/24` 关联资源空态 | 从 Figma 官方集合 `6:22411` 只读比较 `key`/`file-lock` 与 `file-diff`/`file-clock`；最终设备凭证选 `key` 节点 `6:27336`，关联变更文档选 `file-diff` 节点 `6:25779`。耐久资产分别为 `cmdb-resource-key.svg`（SHA-256 `38fed185d755b35fc6e18faee8a8f12dc0c900d1dda75639845108c2510ec1f7`）和 `cmdb-resource-file-diff.svg`（`45089b76e5c9cd819b45c4184c8f1dd117f57b06a6e1c09d735137535b76c631`），均与 Figma 下载字节一致。GitNexus：`InstanceResourcesTab` LOW/1 个直接消费者，局部 `Section` LOW/1 个直接消费者。 | `CONFIRMED`；实例详情/拓扑相关 9/9、typecheck PASS、定向 lint 0 error；浏览器自动化会话仍在登录页，真实两图标回看 `DEFERRED`，用户已在当前页面确认。无 API、query key、RBAC、数据、路由或写入变更。Documentation Impact Assessment：PRD/ADR/API/数据/迁移/安全/运维均 Not applicable；Current System Baseline `Update`；后续文档任务仍为缺失 code-review checklist。 |
