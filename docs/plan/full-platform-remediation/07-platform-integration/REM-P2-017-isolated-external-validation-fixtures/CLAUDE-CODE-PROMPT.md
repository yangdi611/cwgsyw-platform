# REM-P2-017 执行入口

只处理 `REM-P2-017`：提供开发环境的 SMTP / Prometheus / AI 隔离验证夹具与 AI 临时密钥显式清除合同。先读取根 AGENTS、事件五件套和 L4 证据；修改任何符号前运行 GitNexus upstream impact，提交前运行 detect_changes。

仅修改开发 compose，禁止更改生产 compose、连接真实外部系统、写入密码或 token、直连数据库清理。用产品 API 对临时配置做快照、写入、验证、恢复；完成 compile、当前分支容器健康、SMTP 捕获、Prometheus mock 和 AI mock+清钥复验后回写证据，独立提交并 `--no-ff` 合入 `lint-fix`。
