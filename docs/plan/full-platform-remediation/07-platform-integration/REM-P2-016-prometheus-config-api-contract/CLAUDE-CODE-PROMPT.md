# REM-P2-016 执行入口

只处理 `REM-P2-016`：补齐前端既有 Prometheus 保存请求的后端 typed API 合同。实施前读取根 AGENTS、事件五件套和 L4 Phase 9 失败证据；编辑任何符号前运行 GitNexus upstream impact，提交前运行 detect_changes。

保持 `notification:manage` 权限、三个 key 的最小写入面、HTTP/body 错误合同与原子配置存储。不得连接真实外部 Prometheus、修改 SMTP/AI、扩大白名单或清理全局数据。完成成功/拒绝/匿名/恢复原值的当前分支容器复验并回写证据后，独立提交并 --no-ff 合并到 `lint-fix`。
