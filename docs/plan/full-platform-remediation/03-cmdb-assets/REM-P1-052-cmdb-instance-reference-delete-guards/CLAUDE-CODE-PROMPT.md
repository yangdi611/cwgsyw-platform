# REM-P1-052 执行入口

按 `CODEX-GOAL-PROMPT.md` 从检查点继续。只修复 `CMDB-040` 中 CI 被活动关系、设备、变更文档或日报引用时仍可删除的问题。

保持 HTTP 400、不级联、不自动解除引用、无 schema 迁移。完成 L1-L3、当前分支 backend 真实 API/UI、精确清理、detect、独立提交和 no-ff 合并；合并后同 run 仅重验 `CMDB-040`。
