# REM-P2-021 执行入口

在项目根目录继续唯一事件 `REM-P2-021`。先读取 `AGENTS.md`、本事件五件套、整改索引及 L4 运行记录；仅修改流程实例页面的日报业务跳转呈现。编辑 `InstancesPage` 前重新执行 GitNexus upstream impact。完成 L1-L3、真实当前分支容器 UI 复验、证据与全局进度回写、`detect_changes` 后才能提交并 no-ff 合并 `lint-fix`。不得修改流程历史、权限、数据库或非测试对象。
