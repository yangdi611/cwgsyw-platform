# REM-P1-033 执行提示

先取得用户书面确认的变更文档资源范围矩阵。随后在独立 `codex/rem-p1-033-change-doc-edit-scope` 分支上，对每个拟编辑 Controller/Service/授权符号执行 GitNexus upstream impact；实现最小服务层范围校验并覆盖读、写、删除、提交、CI link、审批、导出及无副作用。所有测试身份和文档必须使用 runId 并仅通过产品 API 清理。
